[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "dpo-providers"
version = "0.1.0"
requires-python = ">=3.11,<3.13"
dependencies = []

[tool.uv]
required-version = ">=0.11.0"

[tool.hatch.build.targets.wheel]
packages = ["src/dpo_providers"]
