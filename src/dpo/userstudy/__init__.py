"""The human study served from a published study export.

A separate instrument from `dpo.annotation`: different question, different
response schema, different participants. Kept apart so neither study's
validator can be satisfied by the other's data.
"""
